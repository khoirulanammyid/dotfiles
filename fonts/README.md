# My Fonts Setup

**Fantasque Sans Mono Nerd Font** — the full family (Regular, Mono, Propo in all weights).

Deployed to `~/.local/share/fonts` (XDG font dir). This dir is auto-scanned by
fontconfig, so the font works with every app out of the box.