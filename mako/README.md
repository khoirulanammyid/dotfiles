# My Mako Setup

Notification daemon (`mako`) config:

- **FantasqueSansM Nerd Font**, Gruvbox colors (`#ebdbb2` on `#2b2421`, flat border)
- Per-urgency styling — low/normal muted, high/critical red border with no timeout
- Icons enabled (32px max)
- Special `app-name=osd` style for the `bin` osd scripts: small top-center toast, 1.2s timeout, progress bar