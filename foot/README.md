# My Foot Setup

Wayland-native terminal (`foot`) config:

- **FantasqueSansM Nerd Font** at 14pt, 4px padding
- **Pastel Gruvbox palette** matching alacritty/kitty (`#d4be98` on `#26211f`), full normal + bright ANSI colors
- Windowed startup