# My Foot Setup

Wayland-native terminal (`foot`) config:

- **FantasqueSansM Nerd Font** at 14pt, 4px padding
- **Pastel Gruvbox palette** (`#d4be98` on `#26211f`), full normal + bright ANSI colors, matching the rest of the setup
- Windowed startup